# Project Name (Weather → Economic Impact)

## Goal
Describe in 1–2 sentences what you are predicting (e.g., electricity demand, crop yield, storm losses)
and how weather variables are used.

## Data
- Weather data:
- Economic/market/outcome data:

## How to run
1) Create environment + install deps:
   - `pip install -r requirements.txt`
2) Build dataset:
   - `python src/build_dataset.py`
3) Train model:
   - `python src/train.py`
4) Evaluate:
   - `python src/evaluate.py`
5) Predict:
   - `python src/predict.py`

## Results
(Add metrics + 1–2 plots here once you have them.)
